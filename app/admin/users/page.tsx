import { notFound } from 'next/navigation';

export default function AdminUsersPage() {
  // Admin UI is out of product scope for now.
  // Archived copy exists under /_archive/legacy-pages.
  return notFound();
}

