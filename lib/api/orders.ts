// LEGACY
// This module is intentionally kept as a stub.
// The legacy implementation lives in `lib/_archive/api/orders.ts`.
// Use `lib/domain/orders/api.ts` instead.
export async function createOrderFromDraft(): Promise<never> {
  throw new Error('LEGACY: use lib/domain/orders/api.ts (createOrder)');
}
